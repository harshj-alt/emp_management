<h2 align="center"> Name: <%= @student.name %></h2>

<div class="well col-xs-8 col-xs-offset-2">
  <h4 class="center texts"><strong>  student's age:</strong></h4>
  <hr>
  <%= simple_format(@student.age) %>
    <div class="student-actions">
    <%= link_to "Edit this student", edit_student_path(@student),class: "btn btn-xs btn-primary
    " %>

<%
=begin%>
 <%= link_to "edit this student's details", edit_student_path(@student) %> | 
<%
=end%>
<%= link_to "delete this student ",student_path(@student), method: :delete, data:{confirm: "are you really sure to delete the sudent ??"}, class: "btn btn-xs btn-danger" %>
<%= link_to "back to Student lists", students_path, class: "btn btn-xs btn-success" %>
    </div>
  </div>